export const metadata = {
  title: "Pitch Guru",
  description: "Pitch Guru App",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
